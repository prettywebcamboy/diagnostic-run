# diagnostic.run

Cold terminal-aesthetic device diagnostics and performance guides.

## Local development

npm install
npm run dev

## Production

npm run build

The production output is `dist/`. Connect the repository to Cloudflare Pages or Workers Static Assets. Build command: `npm run build`; output directory: `dist`.
